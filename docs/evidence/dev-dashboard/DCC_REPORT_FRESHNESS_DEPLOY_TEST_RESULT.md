# DCC Report Freshness — Deploy Test Result

| Test | Ergebnis |
|------|----------|
| `test_dev_dashboard_recent_evidence_v1` (unittest) | **ok** (5/5) |
| pytest subset (runtime venv) | **nicht ausführbar** (pytest fehlt in `/opt/.../venv`) |
| `npm run build` (workspace) | **ok** |
| `npm run test` | **nicht ausgeführt** |

**Status:** `partial`
