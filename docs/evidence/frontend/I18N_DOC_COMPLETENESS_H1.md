# i18n — H.1

**none** — `labelKey`/`iconKey` sind interne Contract-Keys (`statusViewModel.kind.*`), noch nicht in UI/locales verdrahtet.

H.2: Keys in `frontend/src/locales/de*.json` / `en*.json` synchronisieren, falls Komponenten migriert werden.
