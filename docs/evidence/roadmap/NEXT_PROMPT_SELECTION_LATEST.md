# Next Prompt Selection (Latest)

**Selected:** `RESCUE_ISO_OPERATOR_RETRY_CONTROLLED_BUILD_AFTER_ISOHYBRID_FIX`

**Warum:** Isohybrid-Fix (Binary-Paketliste) ist im Repo; Operator muss Tree aktualisieren, `auto/clean` und kontrollierten Build im echten Terminal wiederholen.

**Nicht:** USB-Write, Boot-Test, automatisches `apt install`.

Siehe `NEXT_PROMPT_SELECTION_LATEST.json`.
