# 06 – TTY and FD Result

Keine Diagnose-Erfassung von fd0–2 / Terminalflags.

Incidental (late-evidence): Console-Owner `tui` auf tty1; `whiptail` auf tty1 aktiv. Das bestätigt **nicht** FD-Mismatch und ersetzt den Diagnoselauf nicht.
