# Payload Fix — Release Restore Before Build

**Status:** `release_already_active`

| Feld | Wert |
|------|------|
| `install_profile` | `release` |
| `dev_control_enabled` | `false` |
| Dev-Routen | blockiert (erwartet) |

Kein Profilwechsel nötig — System war bereits auf `release` vor Build-Phasen.

Evidence: `payload_fix_release_restored_before_build_latest.json` (gleich Baseline release).
