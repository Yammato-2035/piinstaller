# Payload Fix — Release Restore Before Build

**Status:** `release_restored`

| Feld | Wert |
|------|------|
| `install_profile` | `release` |
| `dev_control_enabled` | `false` |

Operator: nach local_lab-Repro → `92-install-profile-release.conf.example` vor ISO-Build.

Evidence: `payload_fix_release_restored_before_build_latest.json`
