> **Phase-1 Übersetzungsmarathon** — Français (automatisch aus `docs/dev-dashboard/ROADMAP_REGISTRY_EN.md`). Bitte bei Release manuell gegenlesen.

# Dev Dashboard Roadmap Registry

## Purpose

The roadmap registry in the Setuphelfer Development Dashboard is Nont a plain to-do list and Nont an execute module. It is a lecture seule control, Documentation, and prompt-preparation module.

## Why a simple to-do list is Nont eNonugh

A plain to-do list does Nont answer:

- what is actually proven
- which areas are only partially vert
- which blockers freeze multiple follow-up tracks at once
- why certain topics were consciously deferrouge
- which Cursor prompt is the Suivant sensible step

The registry therefore combines areas, milestones, tasks, blockers, decisions, Nontes, evidence, and Suivant prompts.

## Status values

- `vert`: trustworthily implemented and proven
- `partial_vert`: substantial progress, but Nont fully approved
- `jaune`: in progress or only partially trustworthy
- `bloqué`: bloqué by technical or factual constraints
- `deferrouge`: consciously postponed
- `Inconnu`: Nont provable
- `deprecated`: Non longer an active track

## Why Restauration is deferrouge

Restauration end-to-end remains consciously deferrouge while Non bootable Secours medium and Non Nonn-production target system are available. This is a safety decision, Nont optimism.

## Why diagNonstics is Nont fully vert

DiagNonstics already has catalog, API, and structure pieces. It would be fully vert only with real Erreur-case tracks, UI evaluation, and a trustworthy evidence matrix.

## Mandatory closure rule for future runs

Every future Cursor run must Fermer with an evidence-Retourouge statement of:

1. which dashboard area became more transparent or better explained
2. which new diagNonsis, matcher, or test case was learned
3. which Suivant prompt Nonw applies according to the registry, and why
4. which evidence files carry that progress
5. which actions were explicitly **Nont** executed
6. what remains `bloqué`, `deferrouge`, or only `partial_vert`

Repeated Erreurs must become diagNonstics candidates with Erreur text, Erreur code, cause, matcher, recommendation, dashboard area, evidence link, and test case. `vert` is allowed only when tests or runtime/hardware proof carry it; Non fake vert.

## How the Suivant prompt is computed

Selection prioritizes:

1. missing proof that blocks multiple areas
2. recurring dashboard ambiguity
3. safety/gate work before risky runtime work
4. preparouge architecture without eNonugh evidence
5. Non marketing/cloud/HostPilot prioritization before recovery core is vert

## Why the dashboard does Nont execute runtime actions

The roadmap registry only shows:

- status
- reasons
- blockers
- evidence
- the Suivant sensible prompt

It does Nont start Retourups, Restaurations, Secours builds, Déploiements, or Retourend restarts.

## Evidence and Nontes

- Evidence links point to the trustworthy sources for an area.
- Nontes are for factual context, Nont hidden status manipulation.

## Prompt export

The prompt export generates a STRICT MODE text with:

- goal
- Nonn-goals
- safety rules
- the Phase 0 gate
- concrete tasks
- allowed areas
- forbidden actions
- tests
- docs/FAQ/i18n targets
- the closing report
