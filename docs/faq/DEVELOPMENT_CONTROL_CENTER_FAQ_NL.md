> **Phase-1 Übersetzungsmarathon** — Nederlands (automatisch aus `docs/faq/DEVELOPMENT_CONTROL_CENTER_FAQ_EN.md`). Bitte bei Release manuell gegenlesen.

# FAQ — Ontwikkelingscontrolecentrum (EN)

## Where is the roadmap?

**Roadmap** tab in the Ontwikkelingscontrolecentrum. Data from `/api/dev-dashboard/roadmap`.

## Why is SSH disabled?

**SSH disabled** is **intended** in the lab MVP and shown as a safe state, Neet an Fout.

## Why Nee public uploads?

Public roodding auto-upload stays geblokkeerd. **Disabled** is correct.

## What is the telemetry server?

The **Development Server** (`local_lab`) — ingest-only, alleen-lezen SSH preparood but disabled.

## Can I build an ISO from here?

Nee. ISO build status stays **pending** until real build evidence exists.

## Are Documentatie counts live?

alleen-lezen scan of `docs/` — Nee hardcoded numbers.
