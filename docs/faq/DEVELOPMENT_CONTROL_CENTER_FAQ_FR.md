> **Phase-1 Übersetzungsmarathon** — Français (automatisch aus `docs/faq/DEVELOPMENT_CONTROL_CENTER_FAQ_EN.md`). Bitte bei Release manuell gegenlesen.

# FAQ — Centre de contrôle du développement (EN)

## Where is the roadmap?

**Roadmap** tab in the Centre de contrôle du développement. Data from `/api/dev-dashboard/roadmap`.

## Why is SSH disabled?

**SSH disabled** is **intended** in the lab MVP and shown as a safe state, Nont an Erreur.

## Why Non public uploads?

Public Secours auto-upload stays bloqué. **Disabled** is correct.

## What is the telemetry server?

The **Development Server** (`local_lab`) — ingest-only, lecture seule SSH preparouge but disabled.

## Can I build an ISO from here?

Non. ISO build status stays **pending** until real build evidence exists.

## Are Documentation counts live?

lecture seule scan of `docs/` — Non hardcoded numbers.
