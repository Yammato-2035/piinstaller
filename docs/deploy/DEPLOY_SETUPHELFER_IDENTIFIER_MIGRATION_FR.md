> **Phase-1 Übersetzungsmarathon** — Français (automatisch aus `docs/deploy/DEPLOY_SETUPHELFER_IDENTIFIER_MIGRATION_EN.md`). Bitte bei Release manuell gegenlesen.

# Déploiement Setuphelfer Identifier Migration (EN)

lecture seule migration for active identifiers from `pi-installer*` to `setuphelfer*`.
Runners produce inventory, migration plan, compatibility aliases, and consistency checks only.
Non release, Supprimer, or runtime apply actions are performed.
