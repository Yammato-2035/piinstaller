# Manual Runtime Failure Test Sessions

Concrete manual test sessions derived from failure operator checklists.

- `manual_only`; no automatic execution
- No destructive writes; no auto repair/release/ingestion

API: `POST /api/deploy/runner/manual-runtime/failure-test-sessions`
