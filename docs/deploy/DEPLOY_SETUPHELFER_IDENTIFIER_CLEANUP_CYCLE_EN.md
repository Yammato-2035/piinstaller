# Deploy Setuphelfer Identifier Cleanup Cycle (EN)

Cycle 1: plan from `setuphelfer_safe_rewrite_plan.json` (max 100 entries, scoped to backend/frontend/scripts/config/systemd and selected package/tauri files), apply with backups under `legacy-backups/`, postcheck with fresh inventory and consistency check.

No evidence/history paths, no runtime execution; version stays 1.7.0.
