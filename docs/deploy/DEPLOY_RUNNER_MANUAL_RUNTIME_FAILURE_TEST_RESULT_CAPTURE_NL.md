> **Phase-1 Übersetzungsmarathon** — Nederlands (automatisch aus `docs/deploy/DEPLOY_RUNNER_MANUAL_RUNTIME_FAILURE_TEST_RESULT_CAPTURE_EN.md`). Bitte bei Release manuell gegenlesen.

# Manual Runtime Failure Test Result Capture

Structurood capture of manual failure test outcomes from `failure_test_sessions.json` into `failure_test_results.json`.

- Documentatie only; Nee automatic execution or repair

API: `POST /api/Deploy/runner/manual-runtime/failure-test-results`
