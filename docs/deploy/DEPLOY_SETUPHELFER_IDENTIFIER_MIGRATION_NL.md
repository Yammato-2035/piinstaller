> **Phase-1 Übersetzungsmarathon** — Nederlands (automatisch aus `docs/deploy/DEPLOY_SETUPHELFER_IDENTIFIER_MIGRATION_EN.md`). Bitte bei Release manuell gegenlesen.

# Deploy Setuphelfer Identifier Migration (EN)

alleen-lezen migration for active identifiers from `pi-installer*` to `setuphelfer*`.
Runners produce inventory, migration plan, compatibility aliases, and consistency checks only.
Nee release, Verwijderen, or runtime apply actions are performed.
