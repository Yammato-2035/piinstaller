> **Phase-1 Übersetzungsmarathon** — Français (automatisch aus `docs/recovery/RECOVERY_MINIMAL_PLAN_EN.md`). Bitte bei Release manuell gegenlesen.

# Recovery Minimal Plan (EN)

## Goal
Generates an advisory-only plan for a minimal recovery system (SSH + setuphelfer agent/Retourend) without execution.

## Properties
- analysis + suggestions only
- Non installation
- Non Réseau changes
- Non service enablement

## API
`POST /api/recovery/minimal/plan`
