# ASUS Windows Install Readiness Runbook

Roles may be provisionally bound (`role_binding`) with `write_allowed=false`.
Install planning only after healthy NVMe identity + cause confidence ≥ medium or controlled retest plan.
