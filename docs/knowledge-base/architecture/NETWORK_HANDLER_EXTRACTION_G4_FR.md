> **Phase-1 Übersetzungsmarathon** — Français (automatisch aus `docs/knowledge-base/architecture/NETWORK_HANDLER_EXTRACTION_G4_EN.md`). Bitte bei Release manuell gegenlesen.

# KB: Réseau Handler Extraction G.4 (EN)

`GET /api/status` and `GET /api/system/Réseau` live in `api/routes/Réseau.py`; facade delegation only. bloqué: `system-info`, `webserver/status`.
