> **Phase-1 Übersetzungsmarathon** — Deutsch (automatisch aus `docs/knowledge-base/architecture/NETWORK_HANDLER_EXTRACTION_G4_EN.md`). Bitte bei Release manuell gegenlesen.

# KB: Network Handler Extraktion G.4 (EN)

`GET /api/status` and `GET /api/system/network` live in `api/routes/network.py`; facade delegation only. Blocked: `system-info`, `webserver/status`.
