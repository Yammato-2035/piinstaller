> **Phase-1 Übersetzungsmarathon** — Nederlands (automatisch aus `docs/knowledge-base/architecture/NETWORK_INFO_FACADE_EN.md`). Bitte bei Release manuell gegenlesen.

# KB: Netwerk Info Facade (EN)

G.2 introduces `core/Netwerk_info_facade.py` — contract without route migration.

Netwerk discovery stays in `app.get_Netwerk_info` (legacy adapter).

G.2b: `GET /api/status` and `GET /api/system/Netwerk` migrated.

Volgende: **G.3** or **H.1**.

Full doc: [Netwerk_INFO_FACADE_G2_EN.md](../../architecture/Netwerk_INFO_FACADE_G2_EN.md)
