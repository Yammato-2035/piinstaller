# KB: Network Info Facade

G.2 führt `core/network_info_facade.py` ein — Contract ohne Route-Migration.

Netzwerkdiscovery bleibt in `app.get_network_info` (Legacy-Adapter).

G.2b: `GET /api/status` und `GET /api/system/network` migriert.

Nächster Schritt: **G.3** oder **H.1**.

Vollständig: [NETWORK_INFO_FACADE_G2.md](../../architecture/NETWORK_INFO_FACADE_G2.md)
