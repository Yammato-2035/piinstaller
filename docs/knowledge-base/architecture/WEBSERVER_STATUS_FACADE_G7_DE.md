> **Phase-1 Übersetzungsmarathon** — Deutsch (automatisch aus `docs/knowledge-base/architecture/WEBSERVER_STATUS_FACADE_G7_EN.md`). Bitte bei Release manuell gegenlesen.

# KB: Webserver Stand Fassade G.7 (EN)

`GET /api/webserver/status` → `webserver_status_facade`; network/port via `network_info_facade`. G.5 bypass removed.
