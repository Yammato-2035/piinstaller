# KB: APP Router Slice E.5

Roadmap subroutes live in `dev_dashboard_roadmap.py` and call only `load_roadmap_registry_bundle`.

Full `GET /api/dev-dashboard/roadmap` stays in `app.py` for now (dashboard context).

Details: [APP_ROUTER_SLICE_E5_EN.md](../../architecture/APP_ROUTER_SLICE_E5_EN.md)
