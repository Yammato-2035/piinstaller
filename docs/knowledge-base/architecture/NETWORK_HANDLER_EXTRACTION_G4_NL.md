> **Phase-1 Übersetzungsmarathon** — Nederlands (automatisch aus `docs/knowledge-base/architecture/NETWORK_HANDLER_EXTRACTION_G4_EN.md`). Bitte bei Release manuell gegenlesen.

# KB: Netwerk Handler Extraction G.4 (EN)

`GET /api/status` and `GET /api/system/Netwerk` live in `api/routes/Netwerk.py`; facade delegation only. geblokkeerd: `system-info`, `webserver/status`.
