> **Phase-1 Übersetzungsmarathon** — Français (automatisch aus `docs/knowledge-base/architecture/WEBSERVER_STATUS_FACADE_G7_EN.md`). Bitte bei Release manuell gegenlesen.

# KB: Webserver Status Facade G.7 (EN)

`GET /api/webserver/status` → `webserver_status_facade`; Réseau/port via `Réseau_info_facade`. G.5 bypass removed.
