> **Phase-1 Übersetzungsmarathon** — Français (automatisch aus `docs/knowledge-base/architecture/DEPLOY_RISK_GATE_ROUTER_EXTRACTION_EN.md`). Bitte bei Release manuell gegenlesen.

# Risk-Gate Router D.3 — KB

5 GET routes moved to `routes_risk_gate.py`. `routes.py` −32 lines. `allowed_to_execute` false.

Suivant step: **D.4** evidence router.
