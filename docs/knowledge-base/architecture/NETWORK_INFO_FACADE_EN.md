# KB: Network Info Facade (EN)

G.2 introduces `core/network_info_facade.py` — contract without route migration.

Network discovery stays in `app.get_network_info` (legacy adapter).

G.2b: `GET /api/status` and `GET /api/system/network` migrated.

Next: **G.3** or **H.1**.

Full doc: [NETWORK_INFO_FACADE_G2_EN.md](../../architecture/NETWORK_INFO_FACADE_G2_EN.md)
