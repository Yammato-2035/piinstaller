# Versioning router (D.10) — overview

Eight plan-only POST routes in `routes_versioning.py`. No runner execution; `allowed_to_execute` stays false; paths unchanged under `/api/deploy/...`.

See `docs/architecture/DEPLOY_VERSIONING_ROUTER_EXTRACTION_D10_EN.md`.
