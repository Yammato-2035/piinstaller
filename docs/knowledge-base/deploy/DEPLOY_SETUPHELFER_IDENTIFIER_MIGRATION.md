# Deploy Setuphelfer Identifier Migration

Ziel ist die kontrollierte Umstellung aktiver Runtime-Identifier von `pi-installer*` auf `setuphelfer*`.
Historische Belege bleiben bestehen; aktive Legacy-Identifier werden per Konsistenzcheck blockiert.
