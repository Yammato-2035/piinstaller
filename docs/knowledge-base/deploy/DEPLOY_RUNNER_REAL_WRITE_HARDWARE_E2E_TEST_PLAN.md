# KB: DEPLOY_RUNNER_REAL_WRITE_HARDWARE_E2E_TEST_PLAN

## Ueberblick

Read-only Testdesign fuer den Gap `RUNNER_GAP_REAL_WRITE_HARDWARE_E2E_OPEN`.

## Leitlinie

Der echte Write ist nur als manueller geplanter Schritt modelliert, nicht automatisch ausfuehrbar.

## API

- `POST /api/deploy/runner/real-write-hardware-e2e/test-plan`
