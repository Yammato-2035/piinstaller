[project]
name = "setuphelfer-telemetry-server"
version = "0.1.0-beta"
requires-python = ">=3.11"
dependencies = ["fastapi>=0.110", "uvicorn[standard]>=0.27", "pydantic>=2.0"]

[project.optional-dependencies]
dev = ["httpx>=0.27", "pytest>=8.0"]
