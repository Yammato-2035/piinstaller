# Deploy Runtime Router Extraction (D.11)

Eight read-only/status POST routes in `routes_runtime.py` via `build_plan_only_response`. No runtime changes, no restart, `allowed_to_execute` false.

**Next:** E.1 — `app.py` router slice
