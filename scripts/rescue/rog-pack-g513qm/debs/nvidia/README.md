Offline `.deb` files are built on the operator machine via:

```bash
bash scripts/rescue/build-rog-pack-g513qm.sh --copy-to-stick /path/to/SETUP_LOGS
```

They are intentionally not committed (size / redistribution). Stick copies hold the binaries.
