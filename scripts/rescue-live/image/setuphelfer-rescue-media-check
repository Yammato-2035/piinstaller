#!/bin/bash
# SquashFS read stability check for rescue live medium (ISO hybrid + FAT32 ESP).
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# shellcheck source=setuphelfer-rescue-common.sh
source "${SCRIPT_DIR}/setuphelfer-rescue-common.sh"

setuphelfer_rescue_ensure_state_dir
exec python3 "${SCRIPT_DIR}/setuphelfer-rescue-live-medium-check.py"
