"""Platform-family specific compatibility modules (currently: Raspberry Pi).

Separate from ``backend/modules/raspberry_pi_config.py`` (live Pi *configuration*,
product-app feature) — see docs/evidence/rescue/hardware-compat-001/
HARDWARE_DISCOVERY_IST_AUDIT.md. Everything here is read-only compatibility/boot
assessment for the rescue stick, never a config writer.
"""
