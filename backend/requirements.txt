# PI-Installer Backend – Python 3.9+ (3.12 empfohlen)
# Supply-chain: Version ranges limit drift. For reproducible builds use pip freeze or requirements-lock.txt.

# Web Framework (Ranges so pip-audit/OSV bleiben ohne bekannte CVEs)
starlette>=0.49.1,<0.51
fastapi>=0.128,<0.136

# ASGI Server
uvicorn[standard]>=0.24,<0.25

# Request handling
python-multipart>=0.0.6
httpx>=0.25,<0.28  # FastAPI TestClient (0.28+ bricht sync ASGI)

# Environment & Config
python-dotenv>=1.0
PyYAML>=6.0
Jinja2>=3.1

# Utilities
requests>=2.31
aiofiles>=23
click>=8.0
psutil>=5.9
cryptography>=43
slowapi>=0.1.9

# OLED output (Freenove SSD1306)
luma.oled>=3.13
Pillow>=10.0